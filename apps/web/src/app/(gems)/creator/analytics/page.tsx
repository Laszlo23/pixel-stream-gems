"use client";

import { CreatorStudioStub } from "@/views/CreatorStudioStub";

export default function CreatorAnalyticsPage() {
  return (
    <CreatorStudioStub
      title="Analytics"
      description="Revenue, retention, token holders, pool TVL, and stream concurrency — powered by your indexer + API."
    >
      <p>Export metrics for tax reporting and sponsor decks. No fan-facing analytics UI.</p>
    </CreatorStudioStub>
  );
}
