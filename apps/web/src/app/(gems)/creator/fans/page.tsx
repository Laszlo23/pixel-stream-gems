"use client";

import { CreatorStudioStub } from "@/views/CreatorStudioStub";

export default function CreatorFansPage() {
  return (
    <CreatorStudioStub
      title="Fan management"
      description="CRM-style segments: VIP supporters, token holders, NFT collectors, LPs — target messages per group (backend + privacy policy)."
    >
      <p>Connect wallet snapshots and your Postgres API to build segments. Fans only see messages from your creator account.</p>
    </CreatorStudioStub>
  );
}
